# AGENTS.md

## Scope
This project lives in 01-ai-native-workflow/homework1/. Do not read or
modify files outside this folder.

## Commands
- `uv sync` — install dependencies
- `uv run python manage.py runserver` — start the dev server
- `uv run python manage.py test` — run tests

## Rules
- Dependencies go in `pyproject.toml`. Don't add one without asking.
- Follow the plan in `_docs/plan.md` and the backlog in `backlog.md`.
