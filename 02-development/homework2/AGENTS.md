# Development Guidelines

## Backend

Use `uv` for Python dependency management:

```bash
uv sync
uv add <PACKAGE-NAME>
uv run python <PYTHON-FILE>
```

## Git

Commit code regularly in focused, descriptive commits.
